---
title: Blog
# Turns giscus comments on for every post in this section without touching each
# post's front matter, so new posts are covered by default. `params.comments` is
# off site-wide so that docs pages stay uncommented; this is what opts the blog
# back in. A single post can still override with `comments: false`.
cascade:
  comments: true
---
